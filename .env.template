# Usage: cp .env.template .env  # then fill in your keys
# In production these are stored in Google Secret Manager and injected into
# Cloud Run (see docker/cloud-run-*.yaml), not committed.

# === Supabase ===============================================================
SUPABASE_URL=https://lhquhastwnurmvyrzdeh.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# For applying migrations via psql (Project Settings → Database):
# SUPABASE_DB_URL=postgresql://postgres:...@db.lhquhastwnurmvyrzdeh.supabase.co:5432/postgres

# === LLM (DeepSeek) — powers the bot ========================================
DEEPSEEK_API_KEY=
# Optional overrides for the agent (otherwise agent/config/cli-config.yaml):
# HERMES_MODEL=deepseek-v4-flash
# HERMES_PROVIDER=deepseek

# === Telegram ===============================================================
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=OpenTrainerBot   # used to build the t.me deep link

# === Stripe (web/subscription service) ======================================
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=                       # the recurring price for the subscription
STRIPE_WEBHOOK_SECRET=

# === Product / URLs =========================================================
OPEN_TRAINER_PUBLIC_URL=https://open-trainer.app   # web service base (Stripe redirects)
OPEN_TRAINER_SUBSCRIBE_URL=https://open-trainer.app # shown to unsubscribed users in the bot

# === Hermes runtime =========================================================
HERMES_HOME=/data/hermes
